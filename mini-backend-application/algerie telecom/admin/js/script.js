/*
const tables = document.querySelectorAll('.table');

tables.forEach(table => {
    table.querySelectorAll('.btn-danger').forEach((element) => {
        element.addEventListener('click', function() {
            this.parentElement.parentElement.remove()
        });
    });
});

*/